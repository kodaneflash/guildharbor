import { requireMember } from "@/lib/session";
export default async function TrustedSellersPage() { await requireMember(); return <div className="site-container max-w-3xl space-y-5 py-8"><h1 className="text-display-sm font-bold">Trusted Sellers</h1><section className="surface space-y-3 p-6"><p>No sellers currently qualify for a Trusted Seller award.</p><p>Qualification requires a published, versioned policy based on verified completed transactions, eligible reviews and upheld disputes. Evaluation is unavailable until the approved payment milestone.</p><p>Email verification, community reputation and advertising cannot grant this award.</p></section></div>; }
