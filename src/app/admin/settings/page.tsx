import { StaffPage } from "@/components/staff-page";
export default function AdminSettingsPage() { return <StaffPage kind="settings" title="Platform settings" description="Manage feature flags and bounded operational rate limits. Security ceilings remain fixed in code." />; }
