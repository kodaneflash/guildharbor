import { resourceFiles } from "@/domains/delivery/file-service";
export async function ResourceFiles({ purpose, resourceId }: { purpose: string; resourceId: string }) { const files = await resourceFiles(purpose, resourceId); return files.length ? <ul className="space-y-2" aria-label="Secure attachments">{files.map(file => <li key={file.id} className="flex flex-wrap gap-3"><a className="text-category" href={`/api/files/${file.id}`}>{file.name ?? "Download file"}</a>{["image/webp", "text/plain"].includes(file.mediaType) && <a className="text-category" href={`/api/files/${file.id}?preview=1`} target="_blank" rel="noopener noreferrer">Preview {file.name}</a>}</li>)}</ul> : null; }
